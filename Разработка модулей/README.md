# Отчет по практическим работам

## Проверены
- Блочная модель позиционирования.
- Двумерное преобразование.
- Фоновое изображение.
- Итоговая (оставлена как отдельная ранее существовавшая работа).

## Созданы заново
- Анимация.
- Резюме.
- Таблицы.
- ФлексБох.

## Исправлены
- Блочная модель позиционирования: вынесены стили в `css/style.css`, обновлена структура.
- Двумерное преобразование: приведены задания translate/scale/rotate/skew к одному макету.
- Фоновое изображение: градиентный фон, листья, солнце, облака.
- Удалена служебная папка `.idea` из `Фоновое изображение`.

## Ограничения
- Архивы `.rar` в `Data` не удалось распаковать в текущем окружении (нет утилит `unrar/7z`), поэтому реализация выполнена по текстам `.docx` и имеющимся локальным ресурсам.
